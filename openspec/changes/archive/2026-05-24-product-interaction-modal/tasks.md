# Implementation Tasks

1. [ ] Create `ProductModal.tsx` component with detailed info display.
2. [ ] Add "Add to cart" logic (increment/decrement, add to state) in `ProductModal`.
3. [ ] Add "Leave review" form in `ProductModal`.
4. [ ] Modify `CatalogPage` to trigger `ProductModal` open instead of `useNavigate` to product page.
5. [ ] Ensure `ProductModal` fetches or receives full product details for high-res view.
6. [ ] Remove `ProductDetailPage` and corresponding route from `router.tsx`.
7. [ ] Validate all product interactions work correctly from catalog.
